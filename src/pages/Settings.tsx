import SelectedColorScheme from '../components/Settings/SelectColorScheme';

export default function Settings() {
  return (
    <div className="flex h-full w-screen justify-center items-center p-4">
      <SelectedColorScheme />
    </div>
  );
}
